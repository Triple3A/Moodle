#pragma once
#include "Structures.h"
#include <stdio.h>

void grade(Teacher *teach, std *s_head, char* username, char* _grade);
int show_grade(std *stu, char* username);
void show_grades(std *stu);
